// Deprecated EVM stub — kept only to satisfy imports from legacy pages.
// All actual wallet logic uses Solana wallet-adapter in SolanaProvider.
export function ConnectWallet({ appearance = "default" }: { appearance?: "default" | "warm" }) {
  return <button disabled>Connect Wallet</button>;
}